name: network_request_openbudget
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  cupertino_icons: ^1.0.2
  mask_text_input_formatter: ^2.1.0
  numberpicker: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2
  flutter_lints: ^1.0.0


flutter_icons:
    android: true
    ios: true
    image_path: "assets/icon/Elobod.png"

flutter:

  uses-material-design: true
#  assets:
#    - assets/ca/lets-encrypt-r3.pem

